# Harbor Daily Sync - 2026-07-07

## Lane Sync - harbor-im-gateway - 2026-07-07

### Repo / Branch / PR

- Repo: `Bean-Harbor/HarborGate`
- Branch: `codex/feishu-mail-refresh-token`
- PR: `https://github.com/Bean-Harbor/HarborGate/pull/11`

### Today's Scope

- Closed the Feishu Mail refresh-token path for HarborOps real delivery.
- Kept the public HarborBeacon <-> HarborGate v2.0 delivery contract unchanged.
- Preserved ownership boundaries: HarborGate owns Feishu credentials and provider delivery; HarborBeacon owns delivery intent, approval/audit, and business state; HarborOps owns the operator workflow only.

### Completed

- Hardened HarborGate PR #11 so persisted token state is the long-term refresh-token source of truth.
- Merged PR #11 into `main` with merge commit `00f1a5605bbb7b29b2e95249ad6261e99d9ce378`.
- Built and deployed the merged Gate artifact to `.82`:
  - artifact: `/var/lib/harbor-services/harborops-feishu-refresh-20260706T183104/bin/harboros-im-gate`
  - source merge: `00f1a56`
  - sha256: `821c50995d604aff78d1ef30deec2444a169d2cd03d5b95b1e6c88f155d7f220`
- Reauthorized Feishu Mail with `offline_access` and mail send/read scopes.
- Updated `.82` Gate runtime configuration to refresh-token mode:
  - `FEISHU_MAIL_USER_REFRESH_TOKEN` present as bootstrap seed
  - `FEISHU_MAIL_TOKEN_STATE_PATH=/var/lib/harborgate/feishu-mail-token.json`
  - static `FEISHU_MAIL_USER_ACCESS_TOKEN` removed
  - Feishu app id/secret remain Gate-only configuration
- Verified token state protection on `.82`:
  - token state file exists at `/var/lib/harborgate/feishu-mail-token.json`
  - mode: `0600`
  - owner: `root:root`
- Sent the refresh-mode live smoke through HarborOps -> HarborBeacon -> HarborGate -> Feishu Mail.

### Validation

- Ran before merge:
  - `cargo fmt`
  - `cargo test feishu_mail`
  - `cargo test notification_delivery`
  - `cargo check --bin harboros-im-gate`
  - `git diff --check`
- Live `.82` result:
  - HarborGate active and serving the refreshed binary
  - Gate status reported `auth_mode=user_refresh_token`
  - Gate status reported token state configured
  - HarborOps refresh smoke subject: `[HarborOps Smoke Refresh] .82 refresh-token delivery`
  - draft id: `draft_c53f5c0f18b2`
  - delivery intent id: `deliv_303e7fe0d9aa`
  - Beacon delivery id: `delivery_8d05844b22e0847c1847fe17`
  - HarborOps intent status: `sent`
  - HarborOps backend status: `sent`
  - provider message id present: yes
  - allowlisted Outlook inbox receipt confirmed by user

### IM Seam / Rollback Gate

- Inbound task seam status: unchanged
- Outbound delivery seam status: unchanged v2.0 delivery endpoint; new provider adapter behavior is inside HarborGate
- Redacted gateway status available: yes
- Retrieval launch / handoff pack referenced: n-a
- Explicit `knowledge.search` available: n-a
- NL retrieval fallback canary state: n-a
- Rollback safety status: ready
- HarborBeacon direct platform delivery reintroduced: no
- HarborBeacon direct platform delivery count on canary path = 0: yes
- HarborBeacon raw credential ownership or validation reintroduced: no
- HarborOS scope drift into IM transport ownership: no
- Observability fields seen: `auth_mode=user_refresh_token`, token state configured, `platform=feishu_mail`, provider message id present
- Missing fields or notes: no secret, route key, token, app secret, or full email body was recorded in this closeout

### GitHub Status

- Commit/push status: HarborGate PR #11 merged to `main`
- PR status: merged

### Cross-Lane Impact

- Touched seam: outbound notification provider implementation inside HarborGate
- Required collaborators: HarborBeacon delivery handoff already merged; HarborOps release v0.1.0 already deployed
- Frozen boundary affected: no
- IM v1.5 cutover gate package used: n-a; v2.0 contract remained unchanged

### Risks / Blockers

- Feishu refresh token lifetime observed from OAuth response is seven days; the `.82` route should be monitored across at least one refresh boundary.
- Provider authorization can still fail if Feishu app permissions, mailbox identity, or tenant policy changes.

### Rollback Notes

- First rollback live Gate env from:
  - `/var/backups/harboros-im-gate.env.before-feishu-refresh-20260706T114224Z`
  - `/var/backups/harboros-im-gate.env.before-feishu-mail-app-creds-20260706T114307Z`
- Then point Gate systemd back to the previous approved binary if provider delivery regresses.
- If mainline rollback is needed, revert the HarborGate PR #11 merge commit `00f1a5605bbb7b29b2e95249ad6261e99d9ce378`.
- HarborBeacon and HarborOps do not need rollback for this refresh-token-only change unless independent delivery regressions appear.

### Next Step

- Run one scheduled or manual HarborOps smoke after the first refresh boundary to prove the rotated token state is used instead of the original env seed.
- Then move from rollout proof to HarborOps product workflow hardening.

## Architect Closeout - 2026-07-07

### Merge-Ready

- HarborGate Feishu Mail refresh-token support is merged and live-smoke verified on `.82`.

### Pending

- Observe one post-refresh-cycle send to confirm rotated token state survives beyond the bootstrap seed.
- Decide the next HarborOps product hardening slice after the delivery foundation is stable.

### Blocked

- None for the HarborOps refresh-token delivery path.

### Boundary Check

- Frozen contract changed: no
- Cross-lane routing change introduced: no
- New rollback risk introduced: low; rollback is isolated to Gate env/binary or PR #11 revert

### Release / Cutover View

- Safe to merge today: yes; HarborGate PR #11 is already merged
- Safe to cut over today: yes for refresh-token Gate delivery mode on `.82`, with follow-up monitoring after token rotation
- Highest current risk: provider-side Feishu permission or refresh-token lifecycle drift
- Retrieval handoff notes: n-a

### Next-Day Order

1. Verify one refresh-cycle HarborOps smoke after Gate rotates token state.
2. Record the post-refresh evidence without exposing token, route key, app secret, or full email body.
3. Start the next HarborOps product workflow hardening task only after the refresh-cycle check is clean.
